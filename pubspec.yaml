name: dart_backend_framework
description: A Spring-like, Flutter-simple backend framework for Dart.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  http: ^1.2.2
  meta: ^1.12.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.8
